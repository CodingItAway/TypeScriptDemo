function test(n1: number, n2: number, check: boolean, phrase: String)
{
return n1+n2;
}

var x = 5;
var y =51.5;

console.log(test(x,y,true,"Adit"));

const someSampleObject : {
    name : string,
    age : number
} = {
    name : "Adit",
    age : 31
}

console.log(someSampleObject.name);